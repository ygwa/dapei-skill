---
description: dapei.skill 全局工程核心规则
globs: ["**/*"]
alwaysApply: true
---
# dapei.skill 全局规范

- 架构原则：遵循 DDD (Domain-Driven Design) 分层。
- 用户入口：用户通过 `@dapei ...` 表达工程意图，不直接学习内部脚本。
- 工作流：Workspace Root 使用根级 `codebase/`、`docs/`、`features/`，不要创建嵌套 `workspace/` 运行根。
- Feature：Feature 是执行单元，代码变更应通过 `features/<feature>/repos/<repo>` 下映射的仓库进行。
- 上下文：开始任务前读取 `.agents/skills/dapei-skill/SKILL.md`，再读取相关 `docs/`、`.dapei/` 和 feature context。
- 回报：阶段汇报统一包含 `结论 / 风险 / 待确认 / 下一步`。
