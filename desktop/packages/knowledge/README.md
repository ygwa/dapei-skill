# @dapei/desktop-knowledge

CDR 认知资产浏览基础设施。

```
src/
├── portal/     # PortalBuilder、PortalServer（cdr.doc.generate 产物）
├── assets/     # AssetIndex（cognitive index + docs/as-is）
└── evidence/   # EvidenceResolver（sources[] → 绝对路径）
```
