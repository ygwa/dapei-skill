# Repo Context — `dapei-skill` (for feature/cdr-portal-aggregation)

Date: 2026-06-22

> **Purpose**: per `AGENTS.md` line 49, this is the context-injection artifact generated for a feature on `feature.create`. It links the feature's docs to the cognitive index entries (CDR behavior / state-machine / domain / capability-map / business-rule / profile / entry) that are *relevant* to the feature's repos and keywords.
>
> **This feature is a self-host change** — only the `dapei-skill` repo is in scope (per `feature.yaml.repos[0]`). There are no other repos registered against this feature, so there are no upstream cognitive artifacts to inject.

## Feature dimension reminder

Per `AGENTS.md` § "How Context Injection Works":

> On `feature.create`:
> - The system automatically loads the global cognitive index.
> - Behaviors and state machines matching the specified `repos` or keywords in `objective` are injected into `features/<feature>/context/related-cognitive-context.md`.
> - `features/<feature>/docs/01-current-state.md` includes a reference to this file.
> - `features/<feature>/context/repo-context.md` links to the related cognitive context.

The workspace-dimension `docs/as-is/**` and `.dapei/cognitive/**` are **read-only context** for this feature. Edits to those paths are **out of scope** for Round 1 and must wait for `feature.close` backfill (per `AGENTS.md` § "Knowledge Boundary & Dimension Rules").

## Repos in scope

| Repo | Branch in worktree | Path inside workspace | Pre-existing cognitive assets |
|---|---|---|---|
| dapei-skill | `feature/cdr-portal-aggregation` | `repos/dapei-skill` (symlink of `../dapei-skill-portal-aggregation`) | none — this repo is the platform itself, not a product analyzed by CDR |

`repos/dapei-skill/` is the engine itself. Its own `docs/as-is/` is populated by test fixtures only (e.g., `tests/fixtures/sample-node-repo/docs/as-is/behavior/sample-repo-analysis.yaml` — and that fixture is on the v2.2 schema, see Round 1 issue #6 in `01-current-state.md`).

## Cognitive index linkage

There are no upstream behaviors, state machines, domains, business rules, profiles, or entries for this feature to consume. The CDR artifacts that this feature *produces* (after Round 1 ships) are at the workspace-dimension paths and **must not** be edited from inside the feature (per AGENTS.md § "Knowledge Boundary & Dimension Rules" — item 1):

| Will-be-produced artifact | Path (after Round 1) |
|---|---|
| portal pages | `<workspace>/.dapei/docs-portal/{behaviors,states,domains,capabilities,business-rules,profiles,entries,business-modules}/` |
| L1 portal section | `<workspace>/.dapei/docs-portal/l1/` (auto-folded) |
| Cross-repo portal section | `<workspace>/.dapei/docs-portal/cross-repo/` (auto-folded) |

These are **outputs** the feature's implementation will write. They are not **inputs** the feature consumes.

## Runtime-context header (preview)

When Stage 5 (implementation) starts, the AI session should prepend this header to any `runtime-context.md` it generates for the feature:

```
# Runtime Context — feature/cdr-portal-aggregation

Current Workspace Name: dapei-skill
Workspace Path: /Users/ygwang/Develop/github/dapei-skill-portal-aggregation
Current Feature: cdr-portal-aggregation
Active Stage: implementation (Stage 5)
You are in the Feature Dimension. Do NOT edit global files (docs/as-is/*, .dapei/cognitive/*, .dapei/docs-portal/*).
The only workspace-dimension artifact this feature may *write* during implementation is `<workspace>/.dapei/docs-portal/**` (the doc-gen output). That output is regenerated by `cdr.doc.generate` and is the feature's deliverable, not its input.
Merge back to workspace only on feature.close (Round 1 / Round 2 / Round 3 close).
```

## Self-check before Stage 5

Before any code in Stage 5 touches `packages/doc-gen/src/doc-gen.ts`, the implementation session should:

1. `cd /Users/ygwang/Develop/github/dapei-skill-portal-aggregation && git status` — confirm `?? features/` is the only untracked entry (no accidental edits elsewhere).
2. `wc -l packages/doc-gen/src/doc-gen.ts` — confirm baseline (should be 1057 lines; if different, the design doc's "Files to touch" section may be stale).
3. `grep -n "fold_v08_sections\|buildCrossArtifactIndex\|generateBusinessModulesPage\|generateBehaviorByEntryType\|generateBusinessRulesByKind\|generateEntriesPage\|detectExistingPortalSections" packages/doc-gen/src/doc-gen.ts` — confirm none of these symbols already exist (avoid duplicate-definition conflicts).
4. `cat features/cdr-portal-aggregation/docs/05-task-breakdown.md` — re-read the task list in order before T1.1.
5. Confirm with user that implementation should start (cite `SKILL.md` § 阶段确认点 if the user said "do it all" without naming the gates).
