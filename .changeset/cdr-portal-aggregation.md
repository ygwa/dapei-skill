---
"dapei-skill": patch
---

`cdr.doc.generate` (v1.1.0, additive) now emits cross-artifact aggregation pages and cross-link sections so the CDR documentation portal forms a navigable business view instead of a 1-yaml-per-page mirror. A new `buildCrossArtifactIndex` joins behaviors, domains, state machines, business rules, and capability-map entries into 9 Map-based indexes (D1 reuses `behavior.derived_from[]` — no schema change). Five existing page generators (`generateDomainPage` / `generateCapabilityPage` / `generateBehaviorPage` / `generateStatePage` / `generateBusinessRulePage`) gain cross-link sections from the index (D2: unresolved `behavior_id` renders as `~~id~~ (no behavior document)` instead of silently hiding). Four new aggregation page sets land: `/business-modules/index.md` (D4) rolls up domains + behaviors + rules + state machines + repos; `/behaviors/by-entry-type/<type>.md` (D5) groups behaviors by api/mq/cron/rpc/cache/search/other; `/business-rules/by-kind/<kind>.md` groups rules by invariant/constraint/authorization/sla/compensation; `/entries/<repo>/index.md` (BG-9) renders each repo's confirmed entries catalog with a back-link from behavior pages. v0.5 (`/cross-repo/`) and v0.8 (`/l1/`) portal sections are no longer orphaned — `detectExistingPortalSections` scans the portal dir on disk and folds them into `pages[]`, the sidebar, and the top nav whenever those capabilities ran first (D3 default-on, opt-out via `fold_v08_sections: false`). `inputSchema` of `cdr.doc.generate` gains one optional field. 1 new integration test (10 assertions, all green) plus existing `cdr-vitepress-build.test.mjs` (3/3) and `cdr-v0.8-reverse-cluster.test.mjs` continue to pass.
